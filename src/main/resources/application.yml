server:
  port: 8080

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 500
    jedis:
      pool:
        max-idle: 8
        max-wait: -1
        max-active: 8
        min-idle: 0

